# Server URL for API calls
# This should be accessible from the browser, not a Docker internal hostname
NEXT_PUBLIC_SERVER_URL=http://localhost:5050

# Optional: Frame capture interval in milliseconds (default: 500)
# NEXT_PUBLIC_CAPTURE_INTERVAL=500
